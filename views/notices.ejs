<%- include('partials/header', { title: 'Notice Board' }) %>
    <header class="flex flex-col sm:flex-row justify-between items-start sm:items-center mb-6">
        <h1 class="text-3xl font-bold text-gray-700 mb-2 sm:mb-0">Notice Board</h1>
        <div>
            <a href="/dashboard" class="text-blue-600 hover:underline mr-4">Back to Dashboard</a>
            <button onclick="logout()"
                class="bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded-md transition duration-200">Logout</button>
        </div>
    </header>

<main>
    <div id="noticesContainer" class="space-y-6">
    <p class="text-center text-gray-500">Loading Notices...</p>
</div>
</main>

<script src="/js/notices.js"></script>

<%- include('partials/footer') %>
